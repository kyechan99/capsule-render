export const COLOR_PRESETS = {
  gradient: "gradient",
  auto: "auto",
  timeAuto: "timeAuto",
  timeGradient: "timeGradient",
} as const;

export const COLOR_PRESET_VALUES = Object.values(COLOR_PRESETS);
